<controls:ContentPopup x:Class="Telegram.Views.Popups.TextStylePreviewPopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:common="using:Telegram.Common"
                       xmlns:controls="using:Telegram.Controls"
                       xmlns:cells="using:Telegram.Controls.Cells"
                       xmlns:local="using:Telegram.Views.Popups"
                       mc:Ignorable="d"
                       Padding="0,24,0,0"
                       IsDismissButtonVisible="True"
                       ButtonsLayout="Vertical"
                       PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid>
        <ScrollViewer x:Name="ScrollingHost"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Margin="0,0,0,12">
                    <Border Background="{ThemeResource LayerOnMicaBaseAltFillColorSecondaryBrush}"
                            CornerRadius="48"
                            Width="96"
                            Height="96">
                        <controls:AnimatedImage x:Name="Icon"
                                                AutoPlay="False"
                                                IsCachingEnabled="False"
                                                LoopCount="1"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FrameSize="72,72"
                                                DecodeFrameType="Logical"
                                                Width="72"
                                                Height="72" />
                    </Border>

                    <TextBlock x:Name="Title"
                               Style="{StaticResource PopupTextBlockStyle}"
                               TextAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap"
                               Margin="12,12,12,0" />

                    <TextBlock Text="{CustomResource AIEditorStyleText}"
                               TextAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Style="{StaticResource InfoBodyTextBlockStyle}"
                               Grid.Column="1"
                               Grid.Row="2" />
                </StackPanel>

                <Grid x:Name="ContentRoot"
                      Background="{ThemeResource LayerOnMicaBaseAltFillColorSecondaryBrush}"
                      CornerRadius="16"
                      Margin="12,12,12,8"
                      Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{CustomResource AIEditorBefore}"
                               Style="{StaticResource BaseTextBlockStyle}"
                               Margin="12,8,0,4" />

                    <Button Click="Another_Click"
                            Content="{CustomResource AIEditorAnotherExample}"
                            Style="{StaticResource AccentTextButtonStyle}"
                            HorizontalAlignment="Right"
                            Margin="0,-2,0,-8" />

                    <controls:FormattedTextBlock x:Name="Before"
                                                 Margin="12,0,12,12"
                                                 Grid.Row="1" />

                    <Border BorderBrush="{ThemeResource SettingsItemBorderBrush}"
                            BorderThickness="0,1,0,0"
                            Grid.Row="2" />

                    <TextBlock Text="{CustomResource AIEditorAfter}"
                               Style="{StaticResource BaseTextBlockStyle}"
                               VerticalAlignment="Top"
                               Margin="12,12,0,4"
                               Grid.Row="3" />

                    <controls:FormattedTextBlock x:Name="After"
                                                 Margin="12,0,12,12"
                                                 Grid.Row="4" />

                </Grid>

                <controls:SettingsFooter x:Name="Used"
                                         Click="Used_Click"
                                         IsLink="True"
                                         Margin="24,0,24,24"
                                         Grid.Row="2" />
            </Grid>
        </ScrollViewer>

        <controls:ScrollViewerScrim Background="{ThemeResource ContentDialogTopOverlaySolid}"
                                    ScrollingHost="{x:Bind ScrollingHost}"
                                    BottomInset="0" />
    </Grid>

</controls:ContentPopup>
