<controls:ContentPopup x:Class="Telegram.Views.Popups.SendAudiosPopup"
                       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                       xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                       xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       xmlns:controls="using:Telegram.Controls"
                       xmlns:cells="using:Telegram.Controls.Cells"
                       mc:Ignorable="d"
                       Connected="OnLoaded"
                       Padding="0,24,0,0"
                       IsDismissButtonVisible="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <!-- TODO: search -->
        <!--<TextBox Margin="24,0" />-->
        <Button Click="Local_Click"
                Content="{CustomResource AudioSearchLocal}"
                Style="{StaticResource AccentButtonStyle}"
                HorizontalAlignment="Stretch"
                Margin="24,0" />

        <ListView x:Name="ScrollingHost"
                  IsItemClickEnabled="True"
                  ItemClick="OnItemClick"
                  SelectionMode="None"
                  ChoosingItemContainer="OnChoosingItemContainer"
                  ContainerContentChanging="OnContainerContentChanging"
                  Padding="12,0,12,18"
                  Grid.Row="1">
            <ListView.Header>
                <ListView x:Name="ScrollingHost2"
                          IsItemClickEnabled="True"
                          ItemClick="OnItemClick"
                          SelectionMode="None"
                          ChoosingItemContainer="OnChoosingItemContainer"
                          ContainerContentChanging="OnContainerContentChanging"
                          Padding="0,0,0,6"
                          Grid.Row="4">
                    <ListView.Header>
                        <TextBlock Text="{CustomResource AudioSearchProfile}"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   Margin="12,16,12,0" />
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <cells:PlaybackItemCell Background="Transparent"
                                                    BorderBrush="Transparent" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem"
                               BasedOn="{StaticResource DefaultListViewItemStyle}">
                            <Setter Property="HorizontalContentAlignment"
                                    Value="Stretch" />
                            <Setter Property="Padding"
                                    Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.Footer>
                        <TextBlock Text="{CustomResource AudioSearchChats}"
                                   Style="{StaticResource BaseTextBlockStyle}"
                                   Margin="12,8,12,0" />
                    </ListView.Footer>
                    <ListView.Template>
                        <ControlTemplate TargetType="ListView">
                            <Border BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{TemplateBinding CornerRadius}">
                                <ItemsPresenter Header="{TemplateBinding Header}"
                                                HeaderTemplate="{TemplateBinding HeaderTemplate}"
                                                HeaderTransitions="{TemplateBinding HeaderTransitions}"
                                                Footer="{TemplateBinding Footer}"
                                                FooterTemplate="{TemplateBinding FooterTemplate}"
                                                FooterTransitions="{TemplateBinding FooterTransitions}"
                                                Padding="{TemplateBinding Padding}" />
                            </Border>
                        </ControlTemplate>
                    </ListView.Template>
                </ListView>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <cells:PlaybackItemCell Background="Transparent"
                                            BorderBrush="Transparent" />
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem"
                       BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="HorizontalContentAlignment"
                            Value="Stretch" />
                    <Setter Property="Padding"
                            Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <controls:ScrollViewerScrim Background="{ThemeResource ContentDialogTopOverlaySolid}"
                                    ScrollingHost="{x:Bind ScrollingHost}"
                                    BottomInset="0"
                                    Grid.Row="4" />
    </Grid>
</controls:ContentPopup>
